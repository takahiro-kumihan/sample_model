<div class="container-sm">

  <div class="center-center">
    <p><%= course.title %></p>
    <table class="table">
      <tbody>
        <tr>
          <th scope="col">コース名</th>
          <td><%= course.title %></td>
        </tr>
        <tr>
          <th scope="col">説明</th>
          <td><%= course.discription %></td>
        </tr>
        <tr>
          <th scope="col">価格</th>
          <td><%= course.cost %></td>
        </tr>
      </tbody>
    </table>
  </div>

  <!-- <% if (course.subscribed_account) { %>
    <h4 class="center">
      This course has a <a href="<%= `/subscribers/${ course.subscribed_account }` %>"></a> subscribed account.
    </h4>
  <% } %> -->

  <div class="btn-group btn-sm" role="group">
    <a class="btn btn-outline-success" href="<%=`/courses/${ course._id }/edit`%>" role="button">コースの編集</a>
    <a class="btn btn-outline-success" href="<%= `/courses/${ course._id }/delete?_method=DELETE` %>"
    onclick="return confirm('Are you sure you want to delete this record?')" role="button">コースを削除</a>
    <a class="btn btn-outline-success" href="/courses" role="button">コース一覧へ</a>
  </div>

</div>